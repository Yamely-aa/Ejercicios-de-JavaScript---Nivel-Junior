//Escribe una función que reciba un número como argumento y retorne el doble de ese número.

function obtenerDoble(numero) {
    return numero * 2;
}

const resultado = obtenerDoble(10);
console.log(resultado);

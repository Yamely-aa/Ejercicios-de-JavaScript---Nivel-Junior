//Escribe una función que tome dos números como argumentos y retorne el mayor de los dos.
function mayorDeDos(num1, num2) {
    if (num1 > num2) {
        return num1;
    } else {
        return num2;
    }
}

console.log(mayorDeDos(5, 8)); 
console.log(mayorDeDos(12, 7)); 
